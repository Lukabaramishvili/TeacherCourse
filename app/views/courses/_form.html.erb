<%= form_for @course do |f| %>
<%= f.label :name %>
<%= f.text_field :name %><br>
<br>
<%= f.label :teacher_id%>
<%= f.collection_select :teacher_id, Teacher.all, :id, :name %><br>
<%= f.submit %><br>
<%end %>
